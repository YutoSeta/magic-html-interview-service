{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.magic-html.dev/v1/measurement/rate-limit-response.json",
  "title": "Measurement rate-limit response",
  "type": "object",
  "additionalProperties": false,
  "required": ["message"],
  "properties": {
    "message": {"type": "string", "minLength": 1, "maxLength": 1000}
  },
  "description": "Laravel's current JSON throttle response. It is intentionally separate from Measurement Problem because the throttle middleware renders before the domain exception boundary."
}
